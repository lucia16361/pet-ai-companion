# 宠物AI互动器环境变量配置

复制此文件为 `.env` 并填入你的 API key，即可启用真实 LLM 对话和更强的 AI 肖像生成。

```bash
cp .env.example .env
```

## 对话模型（三选一即可）

### OpenAI
```
OPENAI_API_KEY=sk-your-openai-key
OPENAI_MODEL=gpt-4o-mini
```

### Anthropic Claude
```
ANTHROPIC_API_KEY=sk-ant-your-claude-key
CLAUDE_MODEL=claude-3-5-sonnet-20241022
```

### Google Gemini
```
GOOGLE_API_KEY=your-gemini-key
GEMINI_MODEL=gemini-1.5-flash
```

## 图像生成（图生图）

### OpenAI DALL-E 3
```
OPENAI_API_KEY=sk-your-openai-key
IMAGE_PROVIDER=openai
```

### 其他可选
- Replicate
- Stability AI
- Midjourney（需要单独代理）

## 优先级

对话优先使用 `OPENAI_API_KEY` > `ANTHROPIC_API_KEY` > `GOOGLE_API_KEY` > 规则引擎。
